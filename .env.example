# Copy this file to .env.local and fill in the real value once main-frame
# is deployed. Until this is set, the app runs against the mock auth/data
# layer in src/api/mock.ts.
EXPO_PUBLIC_API_BASE_URL=https://main-frame.vercel.app/api
